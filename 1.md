# 一、创建数据库

## 1. 语法

```sql
CREATE DATABASE test DEFAULT CHARACTER SET utf8;
```

| 内容 | 说明 |
|------|------|
| CREATE DATABASE | 创建数据库关键字 |
| test | 数据库名称 |
| DEFAULT CHARACTER SET | 指定的默认编码类型 |
| utf8 | 指定编码 |

## 2. 查看数据库

```sql
SHOW DATABASES;
```

---

# 二、删除数据库

## 1. 语法

```sql
DROP DATABASE TEST;
```

| 内容 | 说明 |
|------|------|
| DROP DATABASE | 删除数据库关键字 |
| TEST | 数据库名称 |

---

# 三、数据类型

> m 表示显示长度，只有设置 ZEROFILL 时才有作用。

## 1. 整数类型

| 数据类型 | 大小 |
|----------|------|
| tinyint(m) | 1个字节（最常用） |
| smallint(m) | 2个字节 |
| mediumint(m) | 3个字节 |
| int(m) | 4个字节（最常用） |
| bigint(m) | 8个字节 |

## 2. 浮点类型

| 数据类型 | 说明 |
|----------|------|
| float(m,d) | 单精度浮点型，m总个数，d小数位 |
| double(m,d) | 双精度浮点型，m总个数，d小数位 |

## 3. 字符类型

| 数据类型 | 说明 |
|----------|------|
| char(n) | 固定长度，n表示不足时补齐 |
| varchar(n) | 可变长度 |
| text | 不设置长度 |

## 4. 日期类型

| 数据类型 | 格式 |
|----------|------|
| date | YYYY-MM-DD |
| time | HH:MM:SS |
| datetime | YYYY-MM-DD HH:MM:SS |
| timestamp | YYYYMMDD HHMMSS |