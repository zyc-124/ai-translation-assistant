# 一、创建与删除表

## 1. 创建表

```sql
CREATE TABLE 表名 (
    列名 类型,
    列名 类型
);
```

| 内容 | 说明 |
|------|------|
| CREATE TABLE | 创建表关键字 |
| 表名 | 表的名称 |
| 列名 | 字段名称 |
| 类型 | 字段的数据类型 |

查看表：

```sql
SHOW TABLES;
```

---

## 2. 删除表

```sql
DROP TABLE 表名;
```

| 内容 | 说明 |
|------|------|
| DROP TABLE | 删除表关键字 |
| 表名 | 要删除的表名 |

---

# 二、修改表

## 1. 修改表名

```sql
ALTER TABLE 旧表名 RENAME 新表名;
```

| 内容 | 说明 |
|------|------|
| ALTER TABLE | 修改表关键字 |
| RENAME | 修改表名关键字 |
| 旧表名 | 修改前的表名 |
| 新表名 | 修改后的表名 |

## 2. 修改列名

```sql
ALTER TABLE 表名 CHANGE COLUMN 旧列名 新列名 类型;
```

| 内容 | 说明 |
|------|------|
| ALTER TABLE | 修改表关键字 |
| CHANGE COLUMN | 修改列名关键字 |
| 旧列名 | 修改前的列名 |
| 新列名 | 修改后的列名 |
| 类型 | 列的数据类型 |

## 3. 修改列类型

```sql
ALTER TABLE 表名 MODIFY 列名 新类型;
```

| 内容 | 说明 |
|------|------|
| MODIFY | 修改列类型关键字 |
| 列名 | 要修改的列名 |
| 新类型 | 修改后的数据类型 |

## 4. 添加新的列

```sql
ALTER TABLE 表名 ADD COLUMN 新列名 类型;
```

| 内容 | 说明 |
|------|------|
| ADD COLUMN | 添加列关键字 |
| 新列名 | 要添加的列名 |
| 类型 | 新列的数据类型 |

## 5. 删除指定的列

```sql
ALTER TABLE 表名 DROP COLUMN 列名;
```

| 内容 | 说明 |
|------|------|
| DROP COLUMN | 删除列关键字 |
| 列名 | 要删除的列名 |

# 三、约束

## 1.主键约束

```
1、单一主键：使用一个列作为主键列，当该列的值有重复时，则违反唯一约束
2、联合主键：使用多个列作为主键列，当多个列的值都想同时，则违反唯一约束
```
```sql
ALTER TABLE 表名 ADD PRIMARY KEY(列名)
```

| 内容 | 说明 |
|------|------|
| ADD | 添加 |
| PRIMARY KEY | 主键约束 |

### 主键自增长

```
要求：  一个表中只能有一个列为自动增长
        自动增长的列必须是整数类型
        自动增长只能添加到具备主键约束与唯一性约束的列上
        删除主键约束或唯一性约束，如果该列具有自动增长能力，则须先删除自动增长后在删除约束。
```

```sql
语法：
    添加： alter table 表名 modify 主键 类型 auto_increment;
```